import React from 'react';
import renderer from 'react-test-renderer';

import ConnectChart from './connect-chart';

test('<ConnectChart />', () => {
  // console.log(ConnectChart)
  // expect(renderer.create(<ConnectChart />).toJSON()).toMatchSnapshot();
});